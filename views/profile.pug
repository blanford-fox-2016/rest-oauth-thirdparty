//- p id : #{user.id}
//- p Username : #{user.name}
//- p name : #{user.displayName}
//- if user.email
  //- p email : #{user.emails[0].value}
p Halaman Dashboard #{user.provider}
if user.provider === "facebook"
  p Display Name Facebook : #{user.displayName}
  p #{user.name}
else if user.provider === "twitter"
  p Display Name Twitter : #{user.displayName}
  p #{user.name}
a(href="/Logout") Log Out
