extends layout

block content
  #screen
    .signup-container
      form(action='/login' method='post')
        input(type='text', placeholder='username')
        input(type='password', placeholder='password')
        button(type='submit') Login
        a(href="/login/facebook") Log In with facebook
